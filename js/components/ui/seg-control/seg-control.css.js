import { css } from '../../../core/css.js';

/** Estilos del control segmentado (usa las clases base .seg/.seg-opt). */
export const styles = css`
  :host { display: block; }
  .seg { width: 100%; }
`;
